export class User {

  public id: number;
  public name: string;
  public firstName: string;

  constructor( id : number,  name: string, firstName: string) {
    this.id = id;
    this.name = name;
    this.firstName = firstName;
  }
}
