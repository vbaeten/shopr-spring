import {Runtime} from "inspector";
import Timestamp = module
import * as module from "module";

export class Order{

  id:number
  date:Timestamp
  totalPrice:number


  constructor(id?: number, date?: NodeJS.Module, totalPrice?: number) {
    this.id = id;
    this.date = date;
    this.totalPrice = totalPrice;
  }
}
